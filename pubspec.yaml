name: shopnexus_flutter_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  build_runner: any
  freezed: any
  json_serializable: any
  retrofit_generator: any
  riverpod_generator: any

flutter:
  uses-material-design: true

dependency_overrides:
  meta: ^1.18.0